# PowerShell 脚本说明

本文说明 `build.ps1` 和 `flash.ps1` 是怎么写的。目标不是把 PowerShell 全部讲完，而是让新人能看懂这两个脚本，并能照着写新的 Zephyr 项目脚本。

## 两个脚本分别做什么

| 脚本 | 作用 | 最终执行的 Zephyr 命令 |
| --- | --- | --- |
| `build.ps1` | 编译项目 | `west build` |
| `flash.ps1` | 下载固件 | `west flash` |

使用方式：

```powershell
.\build.ps1
.\flash.ps1
```

## build.ps1

完整脚本：

```powershell
param(
    [string]$Board = "mini_stm32h743"
)

$workspaceRoot = Resolve-Path "$PSScriptRoot\.."
$env:ZEPHYR_BASE = Join-Path $workspaceRoot "zephyrproject\zephyr"
$env:ZEPHYR_TOOLCHAIN_VARIANT = "zephyr"
$env:ZEPHYR_SDK_INSTALL_DIR = Join-Path $workspaceRoot "zephyr-sdk-1.0.1\zephyr-sdk-1.0.1"

$buildDir = Join-Path $PSScriptRoot "build\$Board"
$westArgs = @("build", "-b", $Board, $PSScriptRoot, "-d", $buildDir)

& python -m west @westArgs
```

### 1. 定义参数

```powershell
param(
    [string]$Board = "mini_stm32h743"
)
```

`param(...)` 用来定义脚本参数。

这里定义了一个字符串参数 `$Board`，默认值是 `mini_stm32h743`。所以直接执行：

```powershell
.\build.ps1
```

等价于：

```powershell
.\build.ps1 -Board mini_stm32h743
```

以后换板子时，只需要改默认值，或者执行时传入新 board：

```powershell
.\build.ps1 -Board other_board
```

### 2. 找到 workspace 根目录

```powershell
$workspaceRoot = Resolve-Path "$PSScriptRoot\.."
```

`$PSScriptRoot` 是 PowerShell 自动提供的变量，表示当前脚本所在目录。

本项目中：

```text
$PSScriptRoot = C:\Users\river\dev\zephyr_ws\craner_encoder_hub
```

`$PSScriptRoot\..` 表示上一级目录，也就是：

```text
C:\Users\river\dev\zephyr_ws
```

`Resolve-Path` 的作用是把相对路径变成确定的绝对路径。

写脚本时推荐用 `$PSScriptRoot`，不要依赖当前终端所在目录。这样无论从哪里调用脚本，路径都不会乱。

### 3. 设置 Zephyr 环境变量

```powershell
$env:ZEPHYR_BASE = Join-Path $workspaceRoot "zephyrproject\zephyr"
$env:ZEPHYR_TOOLCHAIN_VARIANT = "zephyr"
$env:ZEPHYR_SDK_INSTALL_DIR = Join-Path $workspaceRoot "zephyr-sdk-1.0.1\zephyr-sdk-1.0.1"
```

`$env:变量名 = 值` 表示设置环境变量。

这三行告诉 Zephyr：

| 环境变量 | 含义 |
| --- | --- |
| `ZEPHYR_BASE` | Zephyr 源码目录在哪里 |
| `ZEPHYR_TOOLCHAIN_VARIANT` | 使用哪种工具链 |
| `ZEPHYR_SDK_INSTALL_DIR` | Zephyr SDK 在哪里 |

`Join-Path` 用来拼路径。不要手动拼很长的路径字符串，容易写错斜杠。

### 4. 生成构建目录

```powershell
$buildDir = Join-Path $PSScriptRoot "build\$Board"
```

这行生成构建输出目录。

默认情况下结果是：

```text
C:\Users\river\dev\zephyr_ws\craner_encoder_hub\build\mini_stm32h743
```

这样写的好处是：不同 board 可以有不同的构建目录。

### 5. 组织 west build 参数

```powershell
$westArgs = @("build", "-b", $Board, $PSScriptRoot, "-d", $buildDir)
```

`@(...)` 是 PowerShell 数组。

这里不要写成一个长字符串，推荐写成参数数组。原因是路径里如果有空格，数组传参更安全。

这行等价于准备执行：

```powershell
python -m west build -b mini_stm32h743 <项目目录> -d <构建目录>
```

各参数含义：

| 参数 | 含义 |
| --- | --- |
| `build` | 调用 west 的构建命令 |
| `-b` | 指定 board |
| `$Board` | board 名称 |
| `$PSScriptRoot` | 当前 Zephyr 应用目录 |
| `-d` | 指定构建目录 |
| `$buildDir` | 构建输出目录 |

### 6. 执行命令

```powershell
& python -m west @westArgs
```

`&` 是 PowerShell 的调用运算符，表示执行后面的命令。

`python -m west` 表示通过 Python 运行 west。

`@westArgs` 表示把数组展开成命令行参数。

最终执行效果就是：

```powershell
python -m west build -b mini_stm32h743 C:\Users\river\dev\zephyr_ws\craner_encoder_hub -d C:\Users\river\dev\zephyr_ws\craner_encoder_hub\build\mini_stm32h743
```

## flash.ps1

完整脚本：

```powershell
param(
    [string]$Board = "mini_stm32h743",
    [string]$Runner = "stm32cubeprogrammer"
)

$workspaceRoot = Resolve-Path "$PSScriptRoot\.."
$env:ZEPHYR_BASE = Join-Path $workspaceRoot "zephyrproject\zephyr"
$env:ZEPHYR_TOOLCHAIN_VARIANT = "zephyr"
$env:ZEPHYR_SDK_INSTALL_DIR = Join-Path $workspaceRoot "zephyr-sdk-1.0.1\zephyr-sdk-1.0.1"

$buildDir = Join-Path $PSScriptRoot "build\$Board"
$westArgs = @("flash", "-d", $buildDir, "--runner", $Runner)

& python -m west @westArgs
```

`flash.ps1` 和 `build.ps1` 的结构基本一样，只是 west 命令不同。

### 1. 定义参数

```powershell
param(
    [string]$Board = "mini_stm32h743",
    [string]$Runner = "stm32cubeprogrammer"
)
```

`$Board` 表示下载哪个 board 的构建结果。

`$Runner` 表示使用哪个下载工具。这里默认是：

```text
stm32cubeprogrammer
```

它适合通过 ST-LINK 给 STM32 下载固件。

### 2. 设置环境

```powershell
$workspaceRoot = Resolve-Path "$PSScriptRoot\.."
$env:ZEPHYR_BASE = Join-Path $workspaceRoot "zephyrproject\zephyr"
$env:ZEPHYR_TOOLCHAIN_VARIANT = "zephyr"
$env:ZEPHYR_SDK_INSTALL_DIR = Join-Path $workspaceRoot "zephyr-sdk-1.0.1\zephyr-sdk-1.0.1"
```

这部分和 `build.ps1` 一样。

下载时也保留这些环境变量，是为了保证 `west flash` 使用当前 workspace 的 Zephyr 和 SDK。

### 3. 指定构建目录

```powershell
$buildDir = Join-Path $PSScriptRoot "build\$Board"
```

`west flash` 不是直接拿源码下载，而是从构建目录里找到固件和 runner 配置。

默认构建目录是：

```text
build\mini_stm32h743
```

里面会有：

```text
zephyr\zephyr.hex
zephyr\zephyr.bin
zephyr\runners.yaml
```

### 4. 组织 west flash 参数

```powershell
$westArgs = @("flash", "-d", $buildDir, "--runner", $Runner)
```

这行等价于准备执行：

```powershell
python -m west flash -d build\mini_stm32h743 --runner stm32cubeprogrammer
```

各参数含义：

| 参数 | 含义 |
| --- | --- |
| `flash` | 调用 west 的下载命令 |
| `-d` | 指定构建目录 |
| `$buildDir` | 固件所在构建目录 |
| `--runner` | 指定下载工具 |
| `$Runner` | 默认 `stm32cubeprogrammer` |

### 5. 执行命令

```powershell
& python -m west @westArgs
```

和 `build.ps1` 一样，这行负责真正调用 west。

执行成功时，通常会看到：

```text
File download complete
Start operation achieved successfully
```