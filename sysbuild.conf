# Build MCUboot together with the application.
SB_CONFIG_BOOTLOADER_MCUBOOT=y

# Standard MCUboot OTA layout: boot from slot0, download to slot1, then swap
# slot0 and slot1 through scratch_partition on reboot.
SB_CONFIG_MCUBOOT_MODE_SWAP_SCRATCH=y

# Project production signing uses the common EC P-256 key.
# Keep the private key out of Git and provision it on build/release hosts only.
SB_CONFIG_BOOT_SIGNATURE_TYPE_ECDSA_P256=y
SB_CONFIG_BOOT_SIGNATURE_KEY_FILE="keys/craner_sensor_hub_keyv1_prod_ec256_20260828.pem"
